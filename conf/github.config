/*
 * -------------------------------------------------
 *  luslab/group-nextflow-clip Nextflow config file
 * -------------------------------------------------
 * Config options for running nextflow on github actions
 */

docker.enabled = true

params {
  max_memory = 4.GB
  max_cpus = 1
}