#!/usr/bin/env nextflow

// Specify DSL2
nextflow.preview.dsl = 2

// Cutadapt reusable component
process cutadapt {
    input:

    output:

    shell:
    """
    
    """
}

workflow trimreads {
    input 

}